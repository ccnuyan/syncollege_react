const anonymousStore = {};
const anonymousLoginInfo = {
  success: false,
};

const anonymousRegisterInfo = {
  success: false,
};

export default {
  anonymousStore,
  anonymousLoginInfo,
  anonymousRegisterInfo,
};
